name: Build COLMAP

on:
  workflow_call:
    inputs:
      release_tag:
        description: 'Release tag to upload assets to (empty = no upload)'
        type: string
        default: ''
  push:
    branches: [master, main]
    paths:
      - 'CMakeLists.txt'
      - 'cmake/**'
      - 'vcpkg.json'
      - 'overlay-ports/**'
      - 'third_party/colmap'
      - 'third_party/colmap/**'
      - 'third_party/ceres-solver'
      - 'third_party/ceres-solver/**'
      - 'third_party/vcpkg'
      - 'scripts_windows/**'
      - 'scripts_linux/**'
      - '.github/workflows/build-colmap.yml'
  pull_request:
    branches: [master, main]
  workflow_dispatch:
    inputs:
      cuda:
        description: 'Build with CUDA'
        type: boolean
        default: true
      cudss:
        description: 'Build with cuDSS'
        type: boolean
        default: false

env:
  # vcpkg binary caching via the GitHub Actions cache backend. Workflow-level
  # so the manifest install run by CMake configure (not just Setup vcpkg)
  # sees it. Requires the ACTIONS_* exports step in each job.
  VCPKG_BINARY_SOURCES: 'clear;x-gha,readwrite'

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Take Zero / Intake variant: the ONLY build this fork produces.
          # Caspar is deliberately OFF: Intake solves with the OPENCV camera model, which Caspar
          # does not support, and dropping it also drops the Symforce LGPL-adjacent code from the
          # shipped binary. Ceres master + cuDSS provides the (verified 9.7x) GPU bundle adjustment.
          - os: windows-2022
            cuda: true
            cuda_version: '12.8.1'
            cudss: true
            caspar: false
            gui: false
            name: "Windows CUDA12.8+cuDSS"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Fetch vcpkg history for baseline
        run: git -C third_party/vcpkg fetch --unshallow || true
        shell: bash

      # ==================== Disk Space Cleanup ====================
      - name: Free disk space (Linux)
        if: runner.os == 'Linux'
        run: |
          echo "Disk space before cleanup:"
          df -h
          # Remove unnecessary large packages
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/share/swift
          # Clean apt cache
          sudo apt-get clean
          echo "Disk space after cleanup:"
          df -h

      # ==================== CUDA Setup ====================
      # NVIDIA's package endpoints are intermittently inconsistent -- e.g. the
      # apt index served out of sync with its declared size ("File has
      # unexpected size ... Mirror sync in progress?"), which failed 15 Linux
      # jobs in the v4.1.1 release run. Jimver/cuda-toolkit is a JS action, so
      # nick-fields/retry (which only wraps `command:` shell steps) cannot be
      # used here; the first attempt is therefore allowed to fail soft and a
      # single retry runs after a short delay.
      - name: Install CUDA Toolkit (Windows)
        id: cuda_windows
        if: matrix.cuda && runner.os == 'Windows'
        uses: Jimver/cuda-toolkit@v0.2.30
        continue-on-error: true
        with:
          cuda: ${{ matrix.cuda_version }}
          method: 'network'
          sub-packages: '["nvcc", "nvtx", "cudart", "curand", "curand_dev", "nvrtc", "nvrtc_dev", "nvjitlink", "cublas", "cublas_dev", "cusparse", "cusparse_dev", "cusolver", "cusolver_dev", "cufft", "cufft_dev"]'
          use-github-cache: false
          log-file-suffix: '-${{ matrix.name }}.log'

      - name: Wait before CUDA retry (Windows)
        if: matrix.cuda && runner.os == 'Windows' && steps.cuda_windows.outcome == 'failure'
        shell: bash
        run: |
          echo "CUDA toolkit install failed; retrying after a delay to let transient"
          echo "NVIDIA endpoint/CDN inconsistency clear."
          sleep 90

      - name: Install CUDA Toolkit (Windows, retry)
        if: matrix.cuda && runner.os == 'Windows' && steps.cuda_windows.outcome == 'failure'
        uses: Jimver/cuda-toolkit@v0.2.30
        with:
          cuda: ${{ matrix.cuda_version }}
          method: 'network'
          sub-packages: '["nvcc", "nvtx", "cudart", "curand", "curand_dev", "nvrtc", "nvrtc_dev", "nvjitlink", "cublas", "cublas_dev", "cusparse", "cusparse_dev", "cusolver", "cusolver_dev", "cufft", "cufft_dev"]'
          use-github-cache: false
          log-file-suffix: '-${{ matrix.name }}-retry.log'

      - name: Install CUDA Toolkit (Linux)
        id: cuda_linux
        if: matrix.cuda && runner.os == 'Linux'
        uses: Jimver/cuda-toolkit@v0.2.30
        continue-on-error: true
        with:
          cuda: ${{ matrix.cuda_version }}
          method: 'network'
          linux-local-args: '["--toolkit"]'
          log-file-suffix: '-${{ matrix.name }}.log'

      - name: Wait before CUDA retry (Linux)
        if: matrix.cuda && runner.os == 'Linux' && steps.cuda_linux.outcome == 'failure'
        shell: bash
        run: |
          echo "CUDA toolkit install failed; refreshing apt indices and retrying after"
          echo "a delay to let transient NVIDIA mirror inconsistency clear."
          sleep 90
          sudo apt-get update || true

      - name: Install CUDA Toolkit (Linux, retry)
        if: matrix.cuda && runner.os == 'Linux' && steps.cuda_linux.outcome == 'failure'
        uses: Jimver/cuda-toolkit@v0.2.30
        with:
          cuda: ${{ matrix.cuda_version }}
          method: 'network'
          linux-local-args: '["--toolkit"]'
          log-file-suffix: '-${{ matrix.name }}-retry.log'

      # ==================== cuDSS Setup ====================
      - name: Install cuDSS (Windows)
        if: matrix.cuda && matrix.cudss && runner.os == 'Windows'
        shell: pwsh
        run: |
          # Determine CUDA major version for cuDSS URL
          $cudaVersion = "${{ matrix.cuda_version }}"
          $cudaMajor = $cudaVersion.Split('.')[0]

          Write-Host "Downloading cuDSS for CUDA $cudaMajor..."
          $cudssUrl = "https://developer.download.nvidia.com/compute/cudss/redist/libcudss/windows-x86_64/libcudss-windows-x86_64-0.7.1.4_cuda${cudaMajor}-archive.zip"

          try {
            Invoke-WebRequest -Uri $cudssUrl -OutFile cudss.zip -UseBasicParsing
          } catch {
            Write-Error "Failed to download cuDSS: $_"
            exit 1
          }

          Write-Host "Extracting cuDSS..."
          Expand-Archive cudss.zip -DestinationPath "C:/cudss"

          # Find the extracted directory
          $cudssPath = Get-ChildItem -Path "C:/cudss" -Directory | Select-Object -First 1

          # Validate extraction
          if (-not (Test-Path "$($cudssPath.FullName)/include/cudss.h")) {
            Write-Error "cuDSS extraction failed - cudss.h not found"
            exit 1
          }

          Write-Host "cuDSS installed at: $($cudssPath.FullName)"
          echo "CUDSS_ROOT=$($cudssPath.FullName)" >> $env:GITHUB_ENV

      - name: Install cuDSS (Linux)
        if: matrix.cuda && matrix.cudss && runner.os == 'Linux'
        run: |
          # Determine CUDA major version for cuDSS URL
          CUDA_VERSION="${{ matrix.cuda_version }}"
          CUDA_MAJOR="${CUDA_VERSION%%.*}"

          echo "Downloading cuDSS for CUDA $CUDA_MAJOR..."
          CUDSS_URL="https://developer.download.nvidia.com/compute/cudss/redist/libcudss/linux-x86_64/libcudss-linux-x86_64-0.7.1.4_cuda${CUDA_MAJOR}-archive.tar.xz"

          if ! wget -nv "$CUDSS_URL" -O cudss.tar.xz; then
            echo "ERROR: Failed to download cuDSS"
            exit 1
          fi

          echo "Extracting cuDSS..."
          sudo mkdir -p /opt/nvidia/cudss
          sudo tar -xf cudss.tar.xz -C /opt/nvidia/cudss --strip-components=1

          # Validate extraction
          if [ ! -f "/opt/nvidia/cudss/include/cudss.h" ]; then
            echo "ERROR: cuDSS extraction failed - cudss.h not found"
            exit 1
          fi

          echo "cuDSS installed at: /opt/nvidia/cudss"
          echo "CUDSS_ROOT=/opt/nvidia/cudss" >> $GITHUB_ENV

      # ==================== Build Tools ====================
      - name: Install Ninja (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install ninja -y

      # Setup MSVC environment so cl.exe is in PATH for CUDA/vcpkg builds
      - name: Setup MSVC Developer Command Prompt
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install build dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            curl zip unzip tar pkg-config \
            autoconf autoconf-archive automake libtool make \
            ninja-build ccache \
            libgl-dev libgl1-mesa-dev libglu1-mesa-dev libglew-dev \
            libxmu-dev libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev \
            libomp-dev

      # ==================== Compiler Caching ====================
      - name: Setup ccache
        uses: hendrikmuhs/ccache-action@v1
        with:
          key: ccache-${{ matrix.os }}-${{ matrix.cuda && 'cuda' || 'cpu' }}
          max-size: 2G

      - name: Configure ccache (Linux)
        if: runner.os == 'Linux'
        run: |
          echo "/usr/lib/ccache" >> $GITHUB_PATH

      # ==================== vcpkg Setup ====================
      - name: Export GitHub Actions cache env for vcpkg
        uses: actions/github-script@v7
        with:
          script: |
            core.exportVariable('ACTIONS_CACHE_URL', process.env.ACTIONS_CACHE_URL || '');
            core.exportVariable('ACTIONS_RESULTS_URL', process.env.ACTIONS_RESULTS_URL || '');
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env.ACTIONS_RUNTIME_TOKEN || '');

      - name: Setup vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: '${{ github.workspace }}/third_party/vcpkg'
          vcpkgJsonGlob: 'vcpkg.json'

      # ==================== Build ====================
      - name: Build COLMAP (Windows)
        if: runner.os == 'Windows'
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 240
          max_attempts: 3
          retry_wait_seconds: 30
          shell: pwsh
          command: |
            Write-Host "Building COLMAP with CUDA=${{ matrix.cuda }} Caspar=${{ matrix.caspar }} GUI=${{ matrix.gui }}"
            $params = @{
              Configuration = "Release"
            }
            if ("${{ matrix.cuda }}" -ne "true") {
              $params.NoCuda = $true
            }
            if ("${{ matrix.gui }}" -eq "true") {
              $params.Gui = $true
            }
            if ("${{ matrix.caspar }}" -ne "true") {
              $params.NoCaspar = $true
            }
            .\scripts_windows\build_colmap.ps1 @params

      - name: Build COLMAP (Linux)
        if: runner.os == 'Linux'
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 240
          max_attempts: 3
          retry_wait_seconds: 30
          command: |
            CUDA_FLAG=""
            if [ "${{ matrix.cuda }}" != "true" ]; then
              CUDA_FLAG="--no-cuda"
            fi
            CASPAR_FLAG=""
            if [ "${{ matrix.caspar }}" != "true" ]; then
              CASPAR_FLAG="--no-caspar"
            fi

            echo "Building COLMAP with CUDA=${{ matrix.cuda }} Caspar=${{ matrix.caspar }}"
            chmod +x ./scripts_linux/build_colmap.sh
            ./scripts_linux/build_colmap.sh Release $CUDA_FLAG $CASPAR_FLAG

      # ==================== Bundle vcpkg Dependencies (Windows) ====================
      - name: Bundle vcpkg DLLs (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $installBin = "build/install/colmap/bin"

          # Ensure bin directory exists
          if (-not (Test-Path $installBin)) {
            Write-Host "ERROR: COLMAP bin directory not found at $installBin" -ForegroundColor Red
            exit 1
          }

          # Copy vcpkg dependencies
          $vcpkgBin = "build/vcpkg_installed/x64-windows/bin"
          if (Test-Path "$vcpkgBin/*.dll") {
            Write-Host "Copying vcpkg DLLs from: $vcpkgBin"
            Copy-Item "$vcpkgBin/*.dll" $installBin -Force
          }

          Write-Host "Bundled vcpkg DLLs:" -ForegroundColor Green
          Get-ChildItem "$installBin/*.dll" | Select-Object Name

      # ==================== Bundle CUDA Runtime (Windows) ====================
      - name: Bundle CUDA DLLs (Windows)
        if: matrix.cuda && runner.os == 'Windows'
        shell: pwsh
        run: |
          $installBin = "build/install/colmap/bin"
          $cudaPatterns = @(
            "cudart64_*",
            "cublas64_*",
            "cublasLt64_*",
            "cusparse64_*",
            "cusolver64_*",
            "curand64_*",
            "cufft64_*",
            "nvrtc64_*",
            "nvrtc-builtins64_*",
            "nvJitLink_*"
          )

          $versionParts = "${{ matrix.cuda_version }}".Split(".")
          $cudaMajorMinor = "$($versionParts[0]).$($versionParts[1])"
          $candidateBins = @()
          if ($env:CUDA_PATH) {
            $candidateBins += (Join-Path $env:CUDA_PATH "bin")
          }
          $candidateBins += @(
            "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v$cudaMajorMinor\bin",
            "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.8\bin",
            "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.6\bin",
            "C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.0\bin"
          )
          $candidateBins = $candidateBins | Select-Object -Unique

          $copiedCount = 0
          foreach ($cudaBin in $candidateBins) {
            if (-not (Test-Path $cudaBin)) {
              Write-Host "Skipping missing CUDA bin directory: $cudaBin"
              continue
            }

            Write-Host "Checking CUDA runtime DLLs in: $cudaBin"
            Get-ChildItem -Path $cudaBin -Filter "*.dll" | Where-Object { $_.Name -match "^(cu|nv)" } | Select-Object Name | Format-Table -HideTableHeaders

            $dirCopiedCount = 0
            foreach ($pattern in $cudaPatterns) {
              $dlls = Get-ChildItem -Path $cudaBin -Filter "$pattern.dll" -ErrorAction SilentlyContinue
              foreach ($dll in $dlls) {
                Write-Host "  Copying: $($dll.Name)"
                Copy-Item -LiteralPath $dll.FullName -Destination $installBin -Force
                $copiedCount++
                $dirCopiedCount++
              }
            }

            if ($dirCopiedCount -gt 0) {
              Write-Host "Copied $dirCopiedCount CUDA DLLs from $cudaBin" -ForegroundColor Green
              break
            }

            Write-Host "No matching CUDA runtime DLLs found in: $cudaBin"
          }

          if ($copiedCount -eq 0) {
            Write-Host "ERROR: No CUDA runtime DLLs were copied from any candidate CUDA toolkit path." -ForegroundColor Red
            exit 1
          }

          Write-Host "Copied $copiedCount CUDA DLLs" -ForegroundColor Green
          Write-Host "CUDA/NV DLLs in install bin:" -ForegroundColor Green
          Get-ChildItem -Path $installBin -Filter "*.dll" | Where-Object { $_.Name -match "^(cu|nv)" } | Select-Object Name

      # ==================== Bundle cuDSS ====================
      - name: Bundle cuDSS DLLs (Windows)
        if: matrix.cuda && matrix.cudss && runner.os == 'Windows'
        shell: pwsh
        run: |
          $installBin = "build/install/colmap/bin"

          # Find cuDSS DLLs in CUDSS_ROOT
          $searchDirs = @(
            "$env:CUDSS_ROOT/bin/12",
            "$env:CUDSS_ROOT/bin/13",
            "$env:CUDSS_ROOT/bin"
          )

          foreach ($dir in $searchDirs) {
            if (Test-Path "$dir/cudss*.dll") {
              Write-Host "Copying cuDSS DLLs from: $dir"
              Copy-Item "$dir/*.dll" $installBin -Force
              Get-ChildItem "$installBin/cudss*.dll" | ForEach-Object { Write-Host "  - $($_.Name)" }
              break
            }
          }

      - name: Bundle cuDSS libs (Linux)
        if: matrix.cuda && matrix.cudss && runner.os == 'Linux'
        run: |
          INSTALL_LIB="build/install/colmap/lib"

          for dir in "$CUDSS_ROOT/lib/12" "$CUDSS_ROOT/lib64/12" "$CUDSS_ROOT/lib" "$CUDSS_ROOT/lib64"; do
            if ls "$dir"/libcudss*.so* 1>/dev/null 2>&1; then
              echo "Copying cuDSS libs from: $dir"
              cp -f "$dir"/libcudss*.so* "$INSTALL_LIB/"
              ls -la "$INSTALL_LIB"/libcudss*.so*
              break
            fi
          done

      # ==================== Verify Build ====================
      - name: Verify cuDSS DLLs bundled (Windows)
        if: matrix.cuda && matrix.cudss && runner.os == 'Windows'
        shell: pwsh
        run: |
          $cudssDlls = Get-ChildItem "build/install/colmap/bin/cudss*.dll" -ErrorAction SilentlyContinue
          if (-not $cudssDlls) {
            Write-Host "ERROR: cuDSS DLLs not found in build/install/colmap/bin/" -ForegroundColor Red
            Write-Host "Contents of bin directory:" -ForegroundColor Yellow
            Get-ChildItem "build/install/colmap/bin/*.dll" | Select-Object Name
            Write-Host ""
            Write-Host "CUDSS_ROOT was: $env:CUDSS_ROOT" -ForegroundColor Yellow
            if ($env:CUDSS_ROOT) {
              Write-Host "Contents of CUDSS_ROOT:" -ForegroundColor Yellow
              Get-ChildItem $env:CUDSS_ROOT -Recurse -Name | Select-Object -First 30
            }
            exit 1
          }
          Write-Host "cuDSS DLLs found:" -ForegroundColor Green
          $cudssDlls | ForEach-Object { Write-Host "  - $($_.Name)" }

      - name: Verify cuDSS libs bundled (Linux)
        if: matrix.cuda && matrix.cudss && runner.os == 'Linux'
        run: |
          if ! ls build/install/colmap/lib/libcudss*.so* 1>/dev/null 2>&1; then
            echo "ERROR: cuDSS libraries not found in build/install/colmap/lib/"
            echo "Contents of lib directory:"
            ls -la build/install/colmap/lib/ | head -30
            echo ""
            echo "CUDSS_ROOT was: $CUDSS_ROOT"
            if [ -n "$CUDSS_ROOT" ]; then
              echo "Contents of CUDSS_ROOT:"
              find "$CUDSS_ROOT" -type f | head -30
            fi
            exit 1
          fi
          echo "cuDSS libraries found:"
          ls -la build/install/colmap/lib/libcudss*.so*

      # ==================== Verify Caspar ====================
      - name: Verify Caspar export (Windows)
        if: matrix.caspar && runner.os == 'Windows'
        shell: pwsh
        run: |
          $targets = "build/install/colmap/share/colmap/colmap-targets.cmake"
          $header = "build/install/colmap/include/colmap/thirdparty/Symforce-Caspar/generated/f32/solver.h"

          if (-not (Test-Path $targets)) {
            Write-Host "ERROR: COLMAP targets file not found at $targets" -ForegroundColor Red
            exit 1
          }
          if (-not (Select-String -Path $targets -Pattern "caspar_lib_core" -Quiet)) {
            Write-Host "ERROR: Caspar target is missing from COLMAP export" -ForegroundColor Red
            exit 1
          }
          if (-not (Test-Path $header)) {
            Write-Host "ERROR: Caspar generated headers were not installed at $header" -ForegroundColor Red
            exit 1
          }
          Write-Host "Caspar export verified" -ForegroundColor Green

      - name: Verify Caspar export (Linux)
        if: matrix.caspar && runner.os == 'Linux'
        run: |
          TARGETS="build/install/colmap/share/colmap/colmap-targets.cmake"
          HEADER="build/install/colmap/include/colmap/thirdparty/Symforce-Caspar/generated/f32/solver.h"

          if [ ! -f "$TARGETS" ]; then
            echo "ERROR: COLMAP targets file not found at $TARGETS"
            exit 1
          fi
          if ! grep -q "caspar_lib_core" "$TARGETS"; then
            echo "ERROR: Caspar target is missing from COLMAP export"
            exit 1
          fi
          if [ ! -f "$HEADER" ]; then
            echo "ERROR: Caspar generated headers were not installed at $HEADER"
            exit 1
          fi
          echo "Caspar export verified"

      # ==================== Bundle Qt for GUI builds ====================
      - name: Bundle Qt dependencies (Windows GUI)
        if: matrix.gui && runner.os == 'Windows'
        shell: pwsh
        run: |
          $installBin = "build/install/colmap/bin"
          $colmapExe = "$installBin/colmap.exe"
          $vcpkgInstalled = "build/vcpkg_installed/x64-windows"

          if (-not (Test-Path $colmapExe)) {
            Write-Host "ERROR: colmap.exe not found at $colmapExe" -ForegroundColor Red
            exit 1
          }

          # Try windeployqt first
          $windeployqt = Get-ChildItem -Path $vcpkgInstalled -Recurse -Filter "windeployqt.exe" -ErrorAction SilentlyContinue | Select-Object -First 1

          if ($windeployqt) {
            Write-Host "Found windeployqt: $($windeployqt.FullName)"
            Write-Host "Running windeployqt on colmap.exe..."
            & $windeployqt.FullName $colmapExe --no-translations --no-system-d3d-compiler --no-opengl-sw
            if ($LASTEXITCODE -ne 0) {
              Write-Host "WARNING: windeployqt returned non-zero exit code" -ForegroundColor Yellow
            }
          } else {
            Write-Host "WARNING: windeployqt.exe not found, will copy Qt plugins manually" -ForegroundColor Yellow
          }

          # Manual fallback: copy Qt platform plugin if windeployqt didn't
          $platformsDir = "$installBin/platforms"
          if (-not (Test-Path "$platformsDir/qwindows.dll")) {
            Write-Host "windeployqt did not deploy platforms plugin, copying manually..." -ForegroundColor Yellow

            # Search for qwindows.dll in vcpkg
            $qwindows = Get-ChildItem -Path $vcpkgInstalled -Recurse -Filter "qwindows.dll" -ErrorAction SilentlyContinue |
              Where-Object { $_.DirectoryName -like "*plugins*platforms*" -or $_.DirectoryName -like "*Qt6*plugins*platforms*" } |
              Select-Object -First 1

            if ($qwindows) {
              New-Item -ItemType Directory -Force -Path $platformsDir | Out-Null
              Copy-Item $qwindows.FullName "$platformsDir/qwindows.dll" -Force
              Write-Host "Copied $($qwindows.FullName) -> $platformsDir/qwindows.dll" -ForegroundColor Green
            }

            # Also copy Qt styles plugin if present
            $qstyle = Get-ChildItem -Path $vcpkgInstalled -Recurse -Filter "qwindowsvistastyle.dll" -ErrorAction SilentlyContinue |
              Where-Object { $_.DirectoryName -like "*plugins*styles*" } |
              Select-Object -First 1
            if ($qstyle) {
              $stylesDir = "$installBin/styles"
              New-Item -ItemType Directory -Force -Path $stylesDir | Out-Null
              Copy-Item $qstyle.FullName "$stylesDir/qwindowsvistastyle.dll" -Force
              Write-Host "Copied styles plugin" -ForegroundColor Green
            }
          }

          # Final verification
          if (Test-Path "$platformsDir/qwindows.dll") {
            Write-Host "Qt platforms plugin bundled successfully:" -ForegroundColor Green
            Get-ChildItem $platformsDir
            $global:LASTEXITCODE = 0
          } else {
            Write-Host "ERROR: Qt platforms plugin not found anywhere!" -ForegroundColor Red
            Write-Host "Searched in: $vcpkgInstalled" -ForegroundColor Yellow
            Get-ChildItem -Path $vcpkgInstalled -Recurse -Filter "qwindows*" | Select-Object FullName
            exit 1
          }

      # ==================== Smoke Test ====================
      - name: Smoke test COLMAP binary (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $colmap = "build/install/colmap/bin/colmap.exe"
          $binDir = "build/install/colmap/bin"

          if (-not (Test-Path $colmap)) {
            Write-Host "ERROR: colmap.exe not found at $colmap" -ForegroundColor Red
            exit 1
          }

          # List all DLLs in bin directory for diagnostics
          Write-Host "DLLs in bin directory:" -ForegroundColor Cyan
          Get-ChildItem -Path $binDir -Filter "*.dll" | Select-Object Name | Format-Table -HideTableHeaders

          Write-Host "Testing COLMAP binary..."
          $output = & $colmap --help 2>&1

          # Check if we got meaningful output (not just an error)
          if ($output -match "COLMAP" -or $output -match "Usage" -or $output -match "commands") {
            $output | Select-Object -First 10
            Write-Host "COLMAP smoke test passed" -ForegroundColor Green
          } else {
            Write-Host "ERROR: COLMAP binary failed to run or produced no output" -ForegroundColor Red
            Write-Host "Output was: $output"
            Write-Host ""
            Write-Host "Checking for missing DLL dependencies..." -ForegroundColor Yellow
            # Try to get more info about the failure
            try {
              $proc = Start-Process -FilePath $colmap -ArgumentList "--help" -Wait -PassThru -NoNewWindow -RedirectStandardOutput "stdout.txt" -RedirectStandardError "stderr.txt"
              Write-Host "Exit code: $($proc.ExitCode)"
              if (Test-Path "stdout.txt") { Write-Host "stdout: $(Get-Content stdout.txt -Raw)" }
              if (Test-Path "stderr.txt") { Write-Host "stderr: $(Get-Content stderr.txt -Raw)" }
            } catch {
              Write-Host "Process start failed: $_"
            }
            exit 1
          }

      - name: Smoke test COLMAP binary (Linux)
        if: runner.os == 'Linux'
        run: |
          COLMAP="build/install/colmap/bin/colmap"

          if [ ! -f "$COLMAP" ]; then
            echo "ERROR: colmap binary not found at $COLMAP"
            exit 1
          fi

          echo "Testing COLMAP binary..."
          OUTPUT=$("$COLMAP" --help 2>&1 || true)

          # Check if we got meaningful output (not just an error)
          if echo "$OUTPUT" | grep -qiE "(colmap|usage|commands)"; then
            echo "$OUTPUT" | head -10
            echo "COLMAP smoke test passed"
          else
            echo "ERROR: COLMAP binary failed to run or produced no output"
            echo "Output was: $OUTPUT"
            exit 1
          fi

      # ==================== Artifacts ====================
      - name: Prepare artifact name
        id: artifact-name
        shell: bash
        run: |
          VERSION=""
          if [ -f "build/install/colmap/bin/colmap.exe" ]; then
            VERSION=$(build/install/colmap/bin/colmap.exe version | sed -n 's/^COLMAP \([^ ]*\).*/\1/p' | head -n 1 || true)
          elif [ -f "build/install/colmap/bin/colmap" ]; then
            VERSION=$(build/install/colmap/bin/colmap version | sed -n 's/^COLMAP \([^ ]*\).*/\1/p' | head -n 1 || true)
          fi
          if [ -z "$VERSION" ] && [ -f "third_party/colmap/CMakeLists.txt" ]; then
            VERSION=$(grep -E 'set\(COLMAP_VERSION ' third_party/colmap/CMakeLists.txt | sed -n 's/.*"\([^"]*\)".*/\1/p' | head -n 1)
          fi
          if [ -z "$VERSION" ]; then
            VERSION="unknown"
          fi

          CUDA_SUFFIX="${{ matrix.cuda && 'CUDA' || 'CPU' }}"
          CUDSS_SUFFIX="${{ matrix.cudss && '-cuDSS' || '' }}"
          CASPAR_SUFFIX="${{ matrix.caspar && '-Caspar' || '' }}"
          GUI_SUFFIX="${{ matrix.gui && '-GUI' || '' }}"
          echo "name=COLMAP-${VERSION}-${{ matrix.os }}-${CUDA_SUFFIX}${CUDSS_SUFFIX}${CASPAR_SUFFIX}${GUI_SUFFIX}" >> $GITHUB_OUTPUT

      - name: Generate build metadata
        shell: bash
        run: |
          PY=python3; command -v python3 >/dev/null 2>&1 || PY=python
          "$PY" scripts/emit_build_info.py \
            --output build/install/colmap/build_info.json \
            --colmap-dir third_party/colmap \
            --os "${{ matrix.os }}" \
            --variant "${{ matrix.name }}" \
            --cuda-version "${{ matrix.cuda && matrix.cuda_version || 'none' }}" \
            --cudss-version "${{ matrix.cudss && '0.7.1.4' || '' }}" \
            --caspar "${{ matrix.caspar }}" \
            --cudss "${{ matrix.cudss }}" \
            --gui "${{ matrix.gui }}" \
            --cuda-arch "75;80;86;89;90"

      - name: Upload COLMAP to release (Linux)
        if: inputs.release_tag != '' && runner.os == 'Linux'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          ZIP_NAME="${{ steps.artifact-name.outputs.name }}.zip"
          echo "Packaging $ZIP_NAME..."
          cd build/install/colmap && zip -qr "/tmp/$ZIP_NAME" . && cd -
          echo "Uploading $ZIP_NAME..."
          gh release upload "${{ inputs.release_tag }}" "/tmp/$ZIP_NAME" --clobber --repo "${{ github.repository }}"

      - name: Upload COLMAP to release (Windows)
        if: inputs.release_tag != '' && runner.os == 'Windows'
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $zipName = "${{ steps.artifact-name.outputs.name }}.zip"
          Write-Host "Packaging $zipName..."
          Compress-Archive -Path "build\install\colmap\*" -DestinationPath $zipName -Force
          Write-Host "Uploading $zipName..."
          gh release upload "${{ inputs.release_tag }}" $zipName --clobber --repo "${{ github.repository }}"

      - name: Upload COLMAP artifacts
        if: inputs.release_tag == ''
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.artifact-name.outputs.name }}
          path: |
            build/install/colmap/
          retention-days: 7
          if-no-files-found: error
